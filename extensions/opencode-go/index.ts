import { definePluginEntry } from "crawclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "opencode-go",
  name: "OpenCode Go Provider",
  description: "Bundled OpenCode Go provider metadata plugin",
  register() {},
});
