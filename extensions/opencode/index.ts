import { definePluginEntry } from "crawclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "opencode",
  name: "OpenCode Zen Provider",
  description: "Bundled OpenCode Zen provider metadata plugin",
  register() {},
});
