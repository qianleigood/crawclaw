import { definePluginEntry } from "crawclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "copilot-proxy",
  name: "Copilot Proxy",
  description: "Local Copilot Proxy provider metadata plugin",
  register() {},
});
