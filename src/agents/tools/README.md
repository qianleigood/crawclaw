# Agent Tools

`tools/**` owns the executable tool substrate exposed to the agent runtime.

Keep tool schema, tool runtime, and tool-specific helpers here instead of pushing more wiring into
root `src/agents/*.ts` files.
