---
summary: "CLI reference for `crawclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `crawclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `crawclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `crawclaw clawbot qr` (same behavior as [`crawclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `crawclaw clawbot qr` -> `crawclaw qr`
