import { definePluginEntry } from "crawclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "modelstudio",
  name: "Model Studio Provider",
  description: "Bundled Model Studio provider metadata plugin",
  register() {},
});
