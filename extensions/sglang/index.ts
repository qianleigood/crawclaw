import { definePluginEntry } from "crawclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "sglang",
  name: "SGLang Provider",
  description: "Bundled SGLang provider metadata plugin",
  register() {},
});
