import { definePluginEntry } from "crawclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "synthetic",
  name: "Synthetic Provider",
  description: "Bundled Synthetic provider metadata plugin",
  register() {},
});
