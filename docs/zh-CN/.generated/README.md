---
title: "Generated Docs Artifacts"
summary: "由 repo-owned config schema 和 bundled metadata 生成的 docs baseline artifacts"
x-i18n:
  generated_at: "2026-06-10T12:21:02Z"
  model: codex
  provider: openai
  source_hash: 3a1d429b00a20037f7fde938ac0fea0f28dbac3951c0525ddbd931926d5e10c9
  source_path: .generated/README.md
  workflow: 15
---

# Generated Docs Artifacts

这些 baseline artifacts 由 repo-owned CrawClaw config schema 和 bundled channel/plugin metadata 生成。

- 不要手动编辑 `config-baseline.json`。
- 不要手动编辑 `config-baseline.jsonl`。
- 不要手动编辑 `plugin-dependency-plan.json`。
- 不要手动编辑 `plugin-dependency-plan.jsonl`。
- 使用 `pnpm config:docs:gen` 重新生成 config baseline artifacts。
- 在 CI 或本地使用 `pnpm config:docs:check` 验证 config baseline artifacts。
- 使用 `pnpm plugin-deps:gen` 重新生成 plugin dependency plan artifacts。
- 在 CI 或本地使用 `pnpm plugin-deps:check` 验证 plugin dependency plan artifacts。
