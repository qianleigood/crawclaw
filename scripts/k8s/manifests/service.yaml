apiVersion: v1
kind: Service
metadata:
  name: crawclaw
  labels:
    app: crawclaw
spec:
  type: ClusterIP
  selector:
    app: crawclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
