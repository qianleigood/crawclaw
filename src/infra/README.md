# Infra

The old TypeScript infra helper tree has been removed. Shared product
infrastructure now lives with its Rust owner under `crates/`.

This directory is retained for non-executable metadata only.
