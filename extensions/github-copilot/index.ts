import { definePluginEntry } from "crawclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "github-copilot",
  name: "GitHub Copilot Provider",
  description: "GitHub Copilot provider metadata is owned by the Rust catalog",
  register() {},
});
