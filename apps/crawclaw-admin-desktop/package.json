{
  "name": "crawclaw-admin-desktop",
  "private": true,
  "version": "2026.5.3",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && electron .",
    "stage:runtime": "node ../../scripts/admin-desktop-stage-runtime.mjs",
    "pack": "npm run build && npm run stage:runtime && npm run rebuild:admin-native && electron-builder --dir",
    "dist": "npm run build && npm run stage:runtime && npm run rebuild:admin-native && electron-builder",
    "rebuild:native": "electron-rebuild && npm run rebuild:admin-native",
    "rebuild:admin-native": "electron-rebuild --module-dir ../crawclaw-admin --only better-sqlite3,node-pty,ssh2,cpu-features"
  },
  "dependencies": {
    "electron-log": "^5.4.3",
    "keytar": "^7.9.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.2",
    "electron": "^39.2.4",
    "electron-builder": "^26.0.12",
    "typescript": "~5.9.3"
  }
}
